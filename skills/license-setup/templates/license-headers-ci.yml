name: License headers

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Verify license headers
        run: bash scripts/ensure-license-headers.sh --check
