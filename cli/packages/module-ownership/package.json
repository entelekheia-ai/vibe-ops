{
  "name": "@entelekheia/vibe-ops-module-ownership",
  "version": "0.0.1",
  "description": "vibe-ops ownership — the composed boundary's effective class for a path, and the one writer over it",
  "type": "module",
  "license": "Apache-2.0",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=22.18" },
  "dependencies": {
    "@entelekheia/vibe-ops-core": "^0.0.1",
    "@entelekheia/vibe-ops-harness": "^0.0.1"
  },
  "devDependencies": { "@types/node": "^26.1.1", "typescript": "^7.0.2" }
}
