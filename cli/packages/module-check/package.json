{
  "name": "@entelekheia/vibe-ops-module-check",
  "version": "0.0.1",
  "description": "The governance gate as a vibe-ops module — budget, links, the rules bridge, licence texts",
  "type": "module",
  "license": "Apache-2.0",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist", "sh"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=22.18" },
  "dependencies": { "@entelekheia/vibe-ops-core": "^0.0.1" },
  "devDependencies": { "@types/node": "^26.1.1", "typescript": "^7.0.2" }
}
