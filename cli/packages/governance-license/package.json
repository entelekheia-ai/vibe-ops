{
  "name": "@entelekheia/governance-license",
  "version": "0.1.0",
  "description": "The licence texts this tooling hands out, their pinned sources, the rules for applying one, and the scaffold license-setup writes into a repository",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/entelekheia-ai/vibe-ops.git",
    "directory": "cli/packages/governance-license"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "_comment_files": "The data ships BESIDE dist/ and is resolved from the package root defineGovernance is handed — the same import.meta.url shape @entelekheia/vibe-ops-core uses for queries/. A directory added without a line here is present in a checkout and absent from every install.",
  "files": [
    "dist",
    "type.json",
    "templates",
    "migrations",
    "scaffold",
    "authoring.md",
    "ownership.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.18"
  },
  "dependencies": {
    "@entelekheia/governance-base": "^0.1.0",
    "@entelekheia/vibe-ops-core": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
