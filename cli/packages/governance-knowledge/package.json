{
  "name": "@entelekheia/governance-knowledge",
  "version": "0.2.0",
  "description": "The knowledge governance: what a piece of work taught, as two units sharing one promotion test — log (project/log/, moved whole from @entelekheia/governance-log, ADR-0020) and learning (project/learnings/, opt-in)",
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/entelekheia-ai/vibe-ops.git",
    "directory": "cli/packages/governance-knowledge"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./learning": {
      "types": "./dist/index-learning.d.ts",
      "default": "./dist/index-learning.js"
    }
  },
  "_comment_files": "The data ships BESIDE dist/ and is resolved from the package root defineGovernance is handed — the same import.meta.url shape @entelekheia/vibe-ops-core uses for queries/. A directory added without a line here is present in a checkout and absent from every install.",
  "files": [
    "dist",
    "type.json",
    "templates",
    "migrations",
    "migrations-learning",
    "policy",
    "authoring.md",
    "ownership.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.18"
  },
  "dependencies": {
    "@entelekheia/governance-base": "^0.2.0",
    "@entelekheia/vibe-ops-core": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
