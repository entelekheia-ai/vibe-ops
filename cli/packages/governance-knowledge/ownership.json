{
  "note": "This governance's fragment of the promulgation boundary \u2014 composed with the harness base declaration and every other activated governance's fragment at sync time (Plan-033, ADR-0019). Read, never re-decided at a call site. ONE PACKAGE, TWO UNITS (Plan-040 Track 3, ADR-0020), AND THE FRAGMENT IS THE PACKAGE'S: a fragment is composed once per package, so binding only `log` still brings the `learning` entries in with it. That is the honest reading and it has a consequence worth knowing \u2014 `project/templates/learning.md` is `norm` in a repository that never activated `learning`, so promulgation would overwrite it there. It is the same package; a repository that does not want the learning half does not bind this package at all.",
  "version": 2,
  "paths": [
    {
      "match": "project/templates/log.md",
      "class": "norm",
      "why": "Each declares its own version and each version jump has a migration note. This is the class's clearest case: drift here is what the applied-version map exists to detect."
    },
    {
      "match": "project/log/**",
      "class": "shaped",
      "why": "Two owners, permanently (Plan-031): the tooling owns the structure \u2014 migration may restructure per a recorded note, and the template-version gates read the declared stamp \u2014 while the repository owns every word written under it. Promulgation still never writes here; `shaped` is migration's class, not promulgation's."
    },
    {
      "match": "project/templates/learning.md",
      "class": "norm",
      "why": "Same clearest case as the log template: it declares its own version and each version jump has a migration note."
    },
    {
      "match": "project/learnings/**",
      "class": "shaped",
      "why": "The same two-owner rule as project/log/** (Plan-031): the tooling owns the structure, the repository owns every word. Reached only by a repository that has activated `learning` itself \u2014 most have not, this one included."
    }
  ]
}
