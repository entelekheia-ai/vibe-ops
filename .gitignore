.DS_Store
node_modules/
dist/

# Graphify
graphify-out/

# Clone-local configuration. Layers over the committed vibeops.config.ts and vibeops.config.json; holds
# what is true of one working tree on one machine. The committed counterpart declaring which keys exist
# is vibeops.config.local.example.ts.
vibeops.config.local.ts
vibeops.config.local.mjs
vibeops.config.local.js

# Retired (RFC-0004): the record of what was promulgated here now lives in the committed
# vibeops.config.json, the managed layer — never list that file here. This name stays ignored so a
# leftover copy from before the change is not committed by accident; `harness sync` deletes it.
vibeops.config.local.json

# Gambiarra: the sibling path every repo under this workspace resolves the runner through.
# The runner moved to cli/packages/module-check/sh/; seven repos still look for vibe-ops/scripts/.
# Local symlink, never committed — the real fix is those repos moving to the CLI.
/scripts
/sh
