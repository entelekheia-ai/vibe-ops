---
vibe-ops-template: plan@3
---

# Plan-027: One resolve, and a schema that tells the truth

| Field | Value |
|---|---|
| Status | In Progress |
| Created | 2026-08-13 |
| Author | Danilo Borges |
| Depends on | Plan-026 |
| Related | [Plan-026](./shipped/026-the-commands-the-agent-had-to-work-around.md), which carried the cleanup this plan's two decisions were removed from |

---

## Summary

Two consolidations in the CLI surface change a public contract, so neither belongs in a cleanup pass.
The first is that four commands named `resolve` share one implementation and have quietly diverged;
the second is that the MCP tool schema is generated by unioning every verb's flags, so each tool
advertises arguments that are invalid for most of its commands. Both are visible from the code alone,
both were found while auditing how the tooling is actually invoked, and both need a decision recorded
before any line moves — which is why they were split out of Plan-026 rather than carried inside it.

## Goals

1. Exactly one way to ask where a record type lives, its template, and its next number.
2. An MCP tool schema that accepts what is valid for the command being called, and nothing else.
3. A recorded decision for each, so the shape is not re-argued the next time someone reads the code.

## Scope

### In scope

The two consolidations below, each as its own track, each opening with the decision it requires.

### Out of scope

Everything Plan-026 covers — the read verb, the entry defects, the flag asymmetries between sibling
verbs, the routing of the skills onto MCP, and the gate as a `PostToolUse` hook. This plan is only the
part of that audit that changes a contract.

Also out of scope: cutting a release. The repository is under a deliberate version freeze.

## Design

### One `resolve` or four

`plan resolve`, `task resolve`, `log resolve` and `records resolve` all call `resolveRecord` and
`formatResolved` from `@entelekheia/vibe-ops-records`. `records resolve --type` already accepts
`plan|task`, so two of the four are reachable by two names today.

They have diverged where nobody chose to:

| Verb | Divergence |
|---|---|
| `records resolve` | fails without `--type` — the only one with a required flag |
| `log resolve` | has no numbering at all; `formatResolved` emits no `NEXT=` for it |
| `plan resolve` | adds `PLAN_ACTIVE=`, `PLAN_TERMINAL=`, `LIVING=` |
| `task resolve` | adds `GH_REMOTE=`, `GH_AUTH=` |

**The decision is the direction**, and it is not obvious. Collapsing into `records resolve --type <t>`
gives one verb and one place for the per-type extras, at the cost of a required flag on the most common
call. Keeping the nouns and dropping `--type` reads better at a terminal (`vibe-ops plan resolve`) and
leaves `records` answering only for `adr` and `rfc`, which have no noun — but then the two verbs that
resolve the same thing under different names stay, and the divergence above has no single home to be
fixed in.

A third option deserves to be considered rather than assumed away: keep both surfaces and make one a
declared alias of the other, so the divergence is impossible by construction while both spellings
survive.

### Per-`command` MCP schemas instead of a flag union

`shapeFor` in `cli/packages/cli/src/mcp.ts` builds one static input schema per module by concatenating
the module's flags with every command's flags and de-duplicating. One tool, one schema, whatever verb
is called.

The consequence is that the schema misdescribes what is accepted:

- the `task` tool advertises `plan` and `summary-file` for `resolve` and `guard`, where neither applies
- the `plan` tool advertises `from` and `project-dir` for all five commands, where each applies to one

A schema is the only thing an MCP client reads before choosing arguments, so a schema that lists an
invalid argument is not merely untidy — it is the tool inviting a call that cannot work. This is the
same class of defect as a command returning an empty string: the surface asserting something it does
not mean.

The work is to generate one schema per verb. What that implies for tool identity is the decision: one
tool per noun with a discriminated schema, or one tool per verb (`plan_status`, `plan_close`), which
multiplies the tool count against a listing budget shared with every other installed plugin.

## Tracks

- [x] **Track 1 — One resolve.** Decide the direction, record it in the Decision Log with the option
      not taken and why, then converge the four implementations onto it. Touches
      `cli/packages/module-plan/`, `module-task/`, `module-log/`, `module-records/` and
      `cli/packages/records/src/format.ts`. At the end: asking where a record type lives has one
      spelling, and the per-type extras have one home.
      Task: [tasks/one-resolve-and-the-flag-contract-it-needs.md](../tasks/one-resolve-and-the-flag-contract-it-needs.md)

- [ ] **Track 2 — A schema per verb.** Decide between a discriminated schema per noun and one tool per
      verb, weighing the skill-listing budget, then replace the flag union in
      `cli/packages/cli/src/mcp.ts`. At the end: no tool advertises an argument its command rejects,
      and `cli/packages/cli/test/mcp-nouns.test.ts` asserts it. Task: to be opened.

- [ ] Run `/vibe-ops:close-plan` — retrospective against the goals, the demotion check, the tracking
      issue closed. The plan file itself is kept.

## Success criteria

- Every record type's layout is reachable by exactly one command, and a test asserts the four former
  spellings do not all still exist.
- For each MCP tool, the input schema of a given `command` contains no flag that `runModule` would
  reject for that command — asserted mechanically, not by reading.
- `vibe-ops check .` is green and `npm test` passes.

---

## Decision Log

- Decision: The noun is the spelling. `records resolve` answers for `adr` and `rfc` — the two record
  types with no noun of their own — and no longer for `plan` or `task`. `list`, `census` and `show` keep
  all four.
  Rationale: surveying first narrowed the question. The per-type extras this plan's Design attributes to
  the four verbs are already converged inside `resolveRecord`, which branches on its own `type` argument
  rather than on the caller, so there was no divergence needing a single home — only two names for one
  answer. Given that, the direction is decided by what it costs: collapsing onto `records resolve --type`
  would have broken `plugin/hooks/plan-progress-nudge.sh`, which shells `vibe-ops plan resolve` and
  parses five key names out of its output, plus the `resolve` step in both close skills and the MCP test,
  and would have put a required flag on the most common call. Keeping the nouns costs the two spellings
  nobody uses. The third option this plan floated — a declared alias — was rejected as a new contract
  concept that `--help` and the MCP schema would both have to learn, for four call sites. The narrowing
  applies to `resolve` alone, deliberately: nothing on a noun answers what `list` answers, so converging
  it by symmetry would have removed the only way to ask.
  Date / Author: 2026-08-14 / Danilo Borges

- Decision: A flag declares that it is required and what values it accepts (`ModuleFlag.required`,
  `ModuleFlag.choices`), and this lands with Track 1 rather than Track 2.
  Rationale: `--type`'s requirement and its domain were a hand-written conditional, copied byte-identical
  into two verbs, and a missing flag reported the same message as a misspelt one. Track 1 is where the
  two domains stop being equal, which is the moment two copies stop being harmless — so the declaration
  arrives with the verb that proves it, the same argument Plan-025 recorded when it merged the source
  seam into `harness`. Track 2 then consumes both fields for the schema instead of introducing them for
  a consumer that does not yet exist.
  Date / Author: 2026-08-14 / Danilo Borges

- Decision: These two consolidations are a plan of their own rather than tracks inside Plan-026.
  Rationale: Both change a public contract and they influence each other — what `resolve` returns
  determines what a per-verb schema must declare. Deciding either under the momentum of a cleanup pass
  is how a contract gets set by whoever was mid-refactor.
  Date / Author: 2026-08-13 / Danilo Borges

## Outcomes & Retrospective

<!-- Filled at each track completion. -->

---

## Open questions

- Whether one tool per verb is affordable against the skill-listing budget, which is shared across every
  installed plugin. This is measurable before the decision is made rather than after.

## Related

- [Plan-026](./shipped/026-the-commands-the-agent-had-to-work-around.md) — the cleanup pass these two were removed
  from, and where the measurement that found them is recorded.
